# Dev/CI-only dependencies — not needed for core site generation
# Install with: pip install -r requirements-dev.txt
#
# Core dependencies are in requirements.txt
# Image optimisation extras are in requirements-images.txt

# Spell checking (CI spell-check workflow only)
pyspellchecker==0.9.0

# Lint + tests (python-checks workflow; run locally with `ruff check` / `pytest`)
ruff==0.16.1
pytest==9.1.1
