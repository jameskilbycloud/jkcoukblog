---
title: "VMware Cloud on AWS — Starter Workflow"
author: James Kilby
url: https://jameskilby.co.uk/vmc-analyzer/
---

### Business case

Why VMC on AWS, why not the alternatives, and the discovery work that turns this into a funded project.

Project Reset

New Rename Delete Export Import

Progress is stored per project in this browser only. Export to hand a running checklist to someone else. 

0 / 0

!

Reference Why VMC on AWS 

The comparison the "Business case documented" item below asks you to make explicit — against staying self-managed on-prem, and against re-architecting straight onto cloud-native compute. VMC's pitch is specifically the middle path: AWS's infrastructure and operating model, without giving up the vSphere/NSX platform (and skills, and app compatibility) you already run. 

i

**In short:** self-managed on-prem gives you full control, but every operational burden — procurement, patching, hardware failure, a second DR site — is yours to carry, and that overhead scales with fleet size. Refactoring onto cloud-native compute trades that control for a genuinely elastic, provider-managed platform, but the re-architecture itself commonly runs over budget and behind schedule (see the industry figures further down this tab), with a long, expensive stretch of running two platforms in parallel before a single production workload cuts over. **VMC sits in between:** the same vSphere/NSX/vSAN platform your team already operates, run end-to-end by VMware — hardware, patching, hardened baseline, even burst capacity for maintenance windows — deployed in hours and migrated in days via HCX, so that parallel-running window (and its cost) stays short. The tables below go dimension by dimension; the diagram right after this makes the cost-of-delay point on its own. 

Dual-running cost exposure across three migration paths A schematic timeline comparing how long the legacy platform and a new platform run in parallel — and are both being paid for — under a self-managed hardware refresh, a cloud-native re-architecture, and a VMC migration. The cloud-native path shows the longest overlap; VMC shows the shortest. Legacy platform still running Target platform now running Dual-running window — paying for both Self-managed on-prem (hardware refresh) ! Old + new hardware both powered through cutover/validation — typically weeks. Refactor to cloud-native ! Legacy stays live the entire re-architecture — commonly months to years (see the McKinsey-sourced figures below). VMC migration (HCX) ! SDDC deploys in ~2 hours; HCX migration is typically days — legacy decommissioned fast. Project kickoff Fully cut over Time since kickoff — illustrative, not to scale

Schematic, not measured cost data — bar lengths illustrate the relative _durations_ already cited in the cards on this tab (SDDC deploy/HCX timing above; McKinsey-sourced re-architecture schedule figures in the card below), not a dollar figure. Wider dual-running window = longer stretch of paying full price for both platforms at once, assuming a roughly constant run-rate on each side. 

i

**Coordinated disclosure works differently at fleet scale.** When a vulnerability affecting ESXi, vCenter, NSX, or vSAN is disclosed, VMware remediates every VMC on AWS SDDC across the entire fleet _before_ the patch or advisory goes out publicly. That closes a window self-managed estates live in by default: once a patch ships, threat actors routinely reverse-engineer ("diff") it to build a working exploit within days, then go hunting for whoever hasn't applied it yet — that's the usual "N-day" exploitation race. On VMC, there's no unpatched estate left for that exploit to land on; every SDDC is already fixed by the time the vulnerability becomes public knowledge. Confirm the current disclosure-to-remediation practice against VMware's own security advisories process before quoting it as a hard guarantee — see the card footer below. 

Dimension| Self-managed on-prem| Refactor to cloud-native| VMC on AWS  
---|---|---|---  
Time to first workload live | Hardware procurement lead time (often 8–16+ weeks), then rack/stack/cable and an ESXi/vCenter/ vSAN/NSX build-out before a single VM lands — and the depreciation/power/cooling clock starts well before that. | App-by-app re-architecture, re-testing, and re-certification — usually the longest and most open-ended path of the three, with cloud spend accruing on non-production environments the whole time. | No hardware to buy; the SDDC deploys from the console — VMware's own commonly cited figure is roughly two hours for the initial cluster — with workloads following behind HCX the same week. The subscription clock starts at deployment, not at first-workload-live — see the row below.  
Cost incurred before any workload is live | Hardware capex (and its depreciation clock) starts at delivery, and power/cooling runs through the entire build-out and burn-in period — real, ongoing cost accrues for weeks to months before a single VM is in production, even though most of it is capex already committed rather than new spend. | Usually the largest hidden cost of the three: non-production cloud spend (dev/test/staging, load testing, proof-of-concept environments) accrues for the entire re-architecture period — sometimes months to years — before a production workload ever cuts over. Easy to under-budget because it never shows up as one clean line item. | The subscription bills from the moment the SDDC deploys, not from when a workload actually lands on it — every host is being paid for from day one whether or not it's doing anything yet. The gap is genuinely the shortest of the three (HCX/network-extension work is typically days, not weeks), but it's real and should be budgeted explicitly: factor a realistic commissioning timeline into the Cost & subscription planning card's numbers below, not just the steady-state host count, and reflect it in the on-prem run-rate baseline comparison too.  
Migration mechanism & app risk | N/A — workloads are already there, but every hardware refresh is itself a live-migration project. | App code, storage, and networking all change. RDMs, WSFC clusters, physical device dependencies, and vendor support matrices (Oracle's cloud-licensing stance in particular) often block a straight port and force re-testing at the app layer. | HCX vMotion/bulk migration moves the VM as-is — same OS, same app binaries, same IP (network extension) — near-zero downtime, no app changes required.  
Skills required | vSphere/NSX/vSAN administration at design-and-troubleshoot depth — architecting storage/compute layout, diagnosing hardware/firmware faults, tuning disk groups — not just day-2 operation. | Cloud-native compute/networking/IAM skills the team may not have yet — often the actual project bottleneck, more than the application work itself. | The same vSphere/NSX/vSAN concepts the team already knows, but at a lower bar: day-2 _operate_ skills — provisioning, NSX segment/policy management, workload troubleshooting — rather than _design-and-troubleshoot_ skills, since VMware owns storage/compute architecture and hardware diagnostics. Not a skills reset, and arguably a lower skills floor than running the platform yourself, plus a Cloud Console operations layer on top.  
Hardware & capacity operations | You own procurement, rack/power/cooling, firmware, break-fix, and capacity-headroom planning. | Not applicable at the VM layer — but you now own service quotas/limits, autoscaling configuration, and a much larger surface of managed-service SKUs to keep track of. | VMware owns the physical hosts end-to-end — failed hardware is VMware's ticket, not yours. Elastic DRS adds/removes hosts automatically against utilisation thresholds.  
Compute isolation / noisy-neighbor risk | Your own hosts, contended only by your own workloads and whatever placement/anti-affinity rules you set — but you're also the one who has to catch and fix a badly-behaved VM starving its neighbours. | Most cloud-native compute is multi-tenant under the hood — a well-known caveat where another customer's workload can contend for the same underlying physical resources, even with per-instance limits in place. | Every host is single-tenant, dedicated to you — never shared with another VMware customer's workloads. The performance you size for on the Design & Size tab is the performance you actually get, no multi-tenant contention to budget headroom against on top of your own workloads' demand.  
Patch & CVE exposure | Your own patch cycle and change windows — the usual reason estates run years behind on ESXi/ vCenter versions. | Provider-side patching for managed PaaS components; you still patch anything you run yourself (containers, EC2 guest OS). | VMware patches ESXi, vCenter, NSX, and vSAN on its own cadence — no hypervisor maintenance window to schedule, and no risk of being the estate still running an EOL build nobody got round to upgrading. A non-billable host is temporarily added to the cluster for the duration of the patch so your configured resilience level (e.g. N+1) never dips — you're never asked to run under-covered while a host is pulled for maintenance, and you don't pay for that headroom either.  
Security & compliance posture | Physical security and every compliance attestation (ISO 27001, SOC 2, PCI DSS, etc.) is your own program to build, run, and prove. | Inherits the provider's physical/compliance posture, but the security model itself changes — IAM, security groups, and per-service configuration are a genuinely new set of things to get right. | Inherits AWS's physical and compliance posture _and_ keeps the NSX DFW micro-segmentation model the team already knows; vSAN encrypts data at rest by default. Confirm current attestations and the published SLA on VMware's own trust/status pages before quoting either as fact.  
Vulnerability disclosure & patch-gap exposure | Once a patch or advisory is public, you're racing your own change-control process against attackers who reverse-engineer it — the estate stays a known, addressable target for however long that takes. | Provider-managed components get the same fleet-wide protection VMC offers below; anything you operate yourself (guest OS, containers, self-hosted services) runs the same race as on-prem. | See the callout above — the fleet is remediated ahead of public disclosure, so there's no still-vulnerable VMC estate left for a reverse-engineered exploit to target.  
Host & vCenter administrative access | Full root on ESXi and admin on vCenter — powerful, but every one of those credentials is an insider-threat and phishing target, and nothing stops a host quietly drifting out of hardened baseline one hand-tuned setting at a time. | You hold full admin on whatever compute/IAM layer you build — same trade-off as on-prem, just moved up a level to cloud-native services instead of the hypervisor. | No customer ever gets SSH/root on ESXi or the vCenter appliance's underlying admin account — VMware operates the hardened baseline directly. That's not just fewer credentials to leak: it's what makes the fleet-wide patch guarantee above actually enforceable, since no host can drift out of a config VMware doesn't have to fight a customer's own changes to remediate.  
Compliance audit scope | The full stack — physical security, hypervisor configuration, storage, network — is in scope for every attestation and audit you carry (PCI DSS included): you have to evidence controls on layers most cloud-hosted competitors never have to touch. | Provider infrastructure drops out of scope, but everything you configure yourself (IAM, security groups, service configuration) is now new attack surface an assessor has to evaluate. | Because no customer can touch the hypervisor or vCenter admin account, that whole layer is verifiably out of the customer's control boundary — for a PCI DSS cardholder-data-environment assessment in particular, that's real scope reduction: the assessor evaluates what you actually operate (guest OS, application, workload NSX policy), not a hypervisor you never had access to misconfigure. Confirm current attestations (PCI DSS, ISO 27001, SOC 2) on VMware's own compliance pages — which controls fall on VMware's side of the line is documented there, not here.  
DR / HA | A second site is capex — build, run, and patch an entire second stack to get it. | Native multi-AZ/multi-region patterns are available, but only once the re-architected app actually supports them. | A stretched cluster spans two AZs for HA with no second site to build; VMware Live Recovery or a second SDDC covers DR proper — see the Compliance checklist item below for when this is a hard requirement, not a nice-to-have.  
Cost model | Capex-heavy and lumpy (refresh cycles), on top of the ongoing opex of running it. | Consumption-based once live, but the re-architecture itself is a large, hard-to-size one-off cost that isn't on any cloud bill. | Opex subscription, sized against the estate you already have — see the Cost & subscription planning card below for the actual numbers.  
Networking to stand up | Physical VLANs/routing already exist, but a refresh still means re-cabling and a live cutover of all of it. | A full redesign from scratch — VPCs, subnets, security groups, load balancers, DNS — with a genuinely different mental model, not a port of existing config. | NSX segments and the DFW/MGW/CGW model carry straight across unchanged; only the AWS-side pieces (Connected VPC, route tables, Direct Connect/Transit Connect) are new — see the AWS networking primer card below for exactly what's different.  
Existing tooling & integrations | Backup agents, monitoring, VCF Operations, and CMDB integrations are already wired in. | Most need replacing or re-integrating with cloud-native equivalents (native backup, CloudWatch, a different monitoring agent) as part of the same project. | The same vCenter-pluggable tooling keeps working with little to no change — validate your specific backup product against the VMC-supported list on the Optimise tab rather than assuming every on-prem tool is covered.  
Elasticity / scale flexibility | Fixed until the next refresh cycle — adding real capacity takes months, not minutes. | Elastic by design and the strongest card cloud-native holds — pay only for what's actually running, scale to zero if idle. | Elastic DRS adds/removes hosts automatically, but only within a pool of subscriptions you've already bought — scaling past that pool still needs a new host subscription purchased in advance. Genuinely less elastic than native compute; don't oversell this one.  
Vendor & support model | Server OEM, storage OEM, network vendor, and VMware licensing are separate relationships — you integrate across all of them when something breaks. | AWS supports the infrastructure; you still own support for everything you built on top of it. | One Broadcom support contract covers the SDDC layer end-to-end — hosts, hypervisor, vSAN, NSX — with AWS's infrastructure underneath it, invisible and not something you file a ticket against directly.  
Exit / portability | Workloads stay wherever the hardware is — moving them anywhere is its own project. | The hardest of the three to leave once workloads are rebuilt against provider-specific managed services — that's real lock-in, not a talking point. | Workloads stay in standard OVF/VMDK format throughout — HCX (or a plain vMotion) can move them back on-prem or to another vSphere-based cloud just as easily as it moved them in. Lower lock-in risk than a cloud-native rebuild, mechanically, not as a claim.  
  
#####  Why the operational model can be cheaper — even when the subscription line itself isn't 

The subscription price rarely beats depreciated hardware dollar-for-dollar in a straight sticker-price comparison — that's not where the saving is. It's in the operational overhead that a self-managed estate carries around that price, most of which never shows up on the hardware invoice itself: 

i

**The man-hours gap widens as you scale.** Hardware failure is a numbers game — the larger the fleet, the more failures land on your desk every year, and each one is its own diagnose-RMA-evacuate-replace-revalidate cycle. On-prem, that operational burden scales with host count: a 200-host estate doesn't just have more compute, it has proportionally more failure/support tickets to run, every one of them engineer time. On VMC, none of that reaches you at any scale — VMware detects the failure, evacuates the host, and replaces the hardware without an open ticket in your queue, whether the SDDC is 4 hosts or 400. The engineer-hours a self-managed estate spends on hardware break-fix stays close to flat on VMC as the fleet grows, structurally, not just typically lower — this is the row below where scale matters most. 

Where it shows up| On-prem today| What VMC removes or changes  
---|---|---  
Hypervisor/storage operations labour | Dedicated engineer time patching ESXi/vCenter, administering vSAN or a storage array, and tracking firmware/driver compatibility matrices — ongoing, not one-off. | Absorbed entirely by VMware. That engineer time redirects to application and workload work instead of keeping the platform itself alive.  
Hardware failure & support at scale | Failure rate scales with fleet size — a larger estate means proportionally more RMA cycles, each needing diagnosis, vendor coordination, workload evacuation, and re-validation. The operational burden grows with host count, not just the compute it delivers. | Hardware failure, diagnosis, RMA, and replacement are VMware's responsibility end-to-end at any scale — the engineer-hour cost to you stays effectively flat as the estate grows, instead of scaling with it the way it does on-prem.  
Hardware-refresh capex projects | A 6-12+ month project every 3-5 years — procurement, data migration, cutover risk — often with contractor/consulting spend stacked on top of internal time. | Eliminated. Broadcom replaces the underlying hardware without a customer-run project or a planned downtime window.  
Idle, over-provisioned capacity | Capacity bought years ahead of need, plus N+1/N+2 redundancy, sits mostly idle — still costing power, cooling, and depreciation the whole time it isn't used. | Elastic DRS buys closer to actual demand on a shorter cycle — genuinely less stranded capital, though not instant or unlimited (see the Elasticity row above).  
A second (DR) site | Duplicates every cost above just to sit mostly idle, plus its own patch cycle and failover testing burden. | A stretched cluster or VMware Live Recovery replaces a capex site with an opex line — no second physical stack to build, run, and keep in parity.  
Patch-lag risk | Estates that fall behind on patching — because a downtime window is hard to get approved — carry a real breach/outage risk that rarely appears on a budget line until it's realized. | Removed structurally: VMware patches ESXi/vCenter/NSX/vSAN on its own cadence, with no maintenance window for you to schedule or keep deferring.  
Maintenance-window resilience capacity | To patch a host without dropping below your resilience target (e.g. N+1), you have to own and power that spare host year-round — capacity that sits idle except for the rare window it exists for. | VMware temporarily adds a non-billable host to the cluster for the duration of a patch to hold your resilience level, then removes it once the maintenance completes — you never buy, own, or pay for that headroom yourself.  
Multi-vendor incident coordination | Server OEM vs. storage OEM vs. network vendor vs. VMware licensing — cross-vendor finger-pointing during an incident extends time-to-resolution, and downtime has its own cost. | One Broadcom support contract covers the SDDC stack end-to-end, cutting the coordination overhead exactly when it matters most.  
Facilities | Power, cooling, colo/rack space, and eventual decommissioning/e-waste are a fixed cost line regardless of how busy the estate actually is. | Folded into the subscription price — disappears as a separate line item to track and pay.  
  
None of this means the subscription line item is cheaper than depreciated hardware — it often isn't. The saving shows up in **total cost of ownership** once labour, capex-project cost, idle capacity, and facilities are added back to the on-prem side — which is exactly what the "on-prem run-rate baseline" and "TCO comparison" checklist items below are for. Skip either one and you're comparing a subscription invoice to a hardware invoice, not a real TCO. 

**Who operates what, in one line:** physical hosts, the hypervisor, vSAN, and the NSX control plane are VMware's responsibility; guest OS, applications and data, workload-level NSX policy, backup/DR configuration, and your own AWS IAM are yours — the same split as the shared-responsibility model referenced in the AWS networking primer below. On-prem, all of it is yours. 

**Fits well when:** a data-centre exit or hardware refresh is forcing a near-term date; most of the estate is COTS/legacy apps that don't justify a rewrite; the team's depth is vSphere/NSX rather than cloud-native; or DR/compliance needs a fast, low-risk lift rather than a ground-up redesign. **Think twice when:** the goal is a genuine cloud-native transformation the application footprint already supports (VMC becomes an expensive detour on the way there); the estate is small enough that on-demand native compute is simply cheaper at that scale; or the workload needs to scale to zero — the subscription model always has a minimum host floor to pay for, unlike serverless/native compute. 

Deployment-time, SLA, compliance-attestation, and vulnerability-disclosure/remediation-practice figures above are VMware's own commonly cited numbers, not independently verified here — confirm the current figures against VMware's SLA, trust/compliance, and security advisories pages before using them in a business case. 

Reference Why cloud-native re-architecture projects run over budget and late 

!

**This isn't a rare outcome — it's the industry norm.** A widely cited McKinsey survey of roughly 450 CIOs and IT decision-makers (2021) found **75% of cloud migrations ran over budget** and **37–38% ran behind schedule** , with an estimated **$100 billion** in wasted migration spend projected industry-wide over the following three years. These figures describe cloud migration generally, not VMC specifically, and are reported here via secondary sources (the original McKinsey report could not be fetched directly to confirm methodology first-hand) — treat as directionally credible industry signal, not a project-specific guarantee, and verify current figures before citing them in a business case. 

These overruns aren't random bad luck — they trace back to a small set of recurring root causes, most of which are specific to _re-architecting_ an application for cloud-native, not to migration in general. VMC avoids most of them structurally, by not requiring a re-architecture in the first place. 

Root cause of overrun| Why it hits a cloud-native re-architecture| How VMC avoids it  
---|---|---  
Every dependency becomes a design decision | RDMs, WSFC clusters, physical device dependencies, and vendor licensing terms (Oracle's cloud stance in particular) each force an architectural choice mid-project — discovered as blockers during the build, not scoped before it starts. | HCX moves the VM as-is — same OS, same binaries, same dependencies. Run the RVTools analyzer up front (Assess tab) to surface blockers _before_ wave planning starts, not mid-migration.  
Undocumented dependencies surface late | A re-architecture forces every integration point (what talks to what, on which port, how) to be rediscovered and redesigned — legacy apps routinely have dependencies nobody wrote down. | Nothing about the app or its immediate network context changes, so undocumented dependencies that would break a redesign simply keep working — there's nothing to rediscover.  
Skills gap forces expensive contractors | Cloud-native architecture, IAM, and networking are frequently skills the team doesn't have yet — closing that gap with contractors extends both cost and timeline, and the knowledge often leaves with them. | Needs the same vSphere/NSX/vSAN concepts the team already has, at a day-2 _operate_ level (see the Skills required row above) — a materially smaller gap to close.  
No safe rollback once underway | Once code, data model, and infrastructure have all changed together, reverting to the on-prem original isn't simple — a struggling project faces sunk-cost pressure to push forward rather than stop. | Workloads stay in standard OVF/VMDK the whole time (see the Exit/portability row above) — a wave can be paused, tested, or reverted without an irreversible one-way rebuild behind it.  
Non-production spend accrues invisibly | Dev/test/staging and proof-of-concept environments run for the entire re-architecture period — sometimes months to years — before a production cutover, and rarely show up as one clean budget line (see the Cost incurred before any workload is live row above). | No re-architecture period to carry non-production spend through — HCX/network-extension work is typically days, not months, so the same risk exists but the window it can compound in is far shorter.  
Big-bang cutover risk | A changed application architecture often can't be cut over gradually — the redesign forces large, all-or-nothing go-live events with everything riding on one cutover window. | HCX supports wave-based migration — a handful of non-critical VMs first (see the "Test migration of 2–3 non-critical VMs" checklist item), then subsequent waves by application and criticality, each with its own rollback plan.  
  
Cost/schedule statistics: widely cited third-party industry survey data (McKinsey, via secondary reporting), not sourced from or endorsed by VMware/Broadcom, and not a claim about VMC migrations specifically — VMC's own migration risk is still worth planning for using the checklist items below, not assumed away by this comparison. 

Discovery Business case & scope 

The phase before any technical planning — if you're new to VMC on AWS, this is where "how much will this actually cost and is it worth doing" gets answered, before a single host is sized. 

  * Business case documentedWhy VMC on AWS over a hardware refresh, a lift-and-shift to native cloud, or staying put — the driver (data centre exit, hardware EOL, capex-to-opex, DR strategy) shapes everything downstream.
  * Current on-prem run-rate captured as a cost baselineHardware refresh/depreciation, colo/power/cooling, support contracts, and existing VMware licensing — without this there's nothing to compare the Design & Size tab's cost estimate against.
  * Executive sponsor and budget owner identifiedSomeone who can approve the subscription commitment and sign off on ancillary/overage charges as they land.
  * Migration scope and success criteria definedWhich workloads are in scope (and explicitly out of scope), and what "done" looks like — not just "move everything".Download scope template (.csv)
  * Timeline driver identifiedOn-prem contract expiry, hardware end-of-life, data centre exit date, or a compliance deadline — whatever is forcing the date drives wave planning later.
  * Compliance and data residency requirements gatheredDetermines which AWS region(s) are viable and whether a stretched cluster or VMware Live Recovery is a requirement, not an option.
  * Existing licensing inventory collectedWindows Server, SQL Server edition/CALs, Oracle, and other ISV licensing — affects both migration approach (Oracle's cloud licensing stance in particular needs checking) and the SPLA/BYOL cost line in the Design & Size tab.
  * Required Broadcom support tier decidedConfirm response-time/severity requirements against what's actually offered before it's a surprise during a P1.

Reference Official resources — for a seller new to VMC 

If you sell across the wider Broadcom/VMware portfolio but haven't worked a VMC on AWS deal before, these are the official pages worth bookmarking — orientation first, then the commercial mechanics, then the specifics a technical buyer will ask about. All external; none of this is specific to this tool. 

Resource| Why it's useful before a VMC call  
---|---  
[VMware Cloud on AWS — Getting Started](https://techdocs.broadcom.com/us/en/vmware-cis/cloud/vmware-cloud-on-aws/SaaS/vmware-cloud-on-aws-getting-started.html) | The official onboarding landing page — Learn / Plan / Prepare / Operate. Fastest way to get oriented on the product before a first customer conversation.  
[VMware Cloud on AWS — FAQ](https://www.vmware.com/docs/vmware-cloud-on-aws-frequently-asked-questions) | A long, plain-language Q&A PDF covering what it is, how it's architected, and common objections — good prep reading before a discovery call, not just a reference to send a customer.  
[Purchase Options for VMware Cloud on AWS](https://techdocs.broadcom.com/us/en/vmware-cis/cloud/vmware-cloud-on-aws/SaaS/vmware-cloud-on-aws-getting-started/prepare/purchase-options-for-vmware-cloud-on-aws.html) | The authoritative statement of the current commercial model — subscription-only, no on-demand, purchased in advance of deployment/scaling. Confirms the same facts this toolkit's Discovery/Commercial checklist items are built around; check it for anything more current.  
[AWS Region and Availability Zone Support](https://techdocs.broadcom.com/us/en/vmware-cis/cloud/vmware-cloud-on-aws/SaaS/vmware-cloud-on-aws-getting-started/regions-grid.html) | Not every AWS region runs VMC, and not every VMC region supports every feature (stretched clusters, PCI/IRAP compliance hardening) — check this before promising a region works. This toolkit's own region list is sourced from this exact page.  
[VMware Cloud Sizer](https://vmc.vmware.com/sizer/) | The official, free sizing tool — a second opinion alongside (or instead of) this toolkit's own estimate, and the one a technical buyer or SE will likely already trust.  
[VMware Cloud Sizer API docs](https://developer.broadcom.com/xapis/vmware-cloud-sizer-api/latest/) | For an SE/architect wanting to automate or validate sizing programmatically — this toolkit's own "Validate with official VMware Sizer" feature (Design & Size tab) is built against this exact API.  
[Introduction to HCX](https://www.vmware.com/docs/vmware-cloud-introduction-to-hcx) | HCX is the migration mechanism and a major part of the pitch (near-zero-downtime, no app changes) — this is the technical-depth version; useful once a prospect starts asking "how does the migration actually work."  
[Broadcom Trust Center](https://www.broadcom.com/support/trust-center) | Start here for compliance/security questions (ISO 27001/27017/27018, SOC 2/3, PCI DSS) rather than quoting a certification from memory — confirm current scope and attestations before citing any of them to a customer.  
  
Every link above was checked live while writing this card, but Broadcom reorganizes these URLs from time to time (the docs.vmware.com → techdocs.broadcom.com move being the big recent example) — if one 404s, search the page title on techdocs.broadcom.com rather than assuming the content is gone. For internal sales enablement (battlecards, competitive positioning, partner pricing) go through the Broadcom Partner/Sales Portal directly — those live behind authentication and aren't linkable here.